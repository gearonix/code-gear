version: '3.9'

services:
  mysql:
    image: mysql:latest
    restart: always
    container_name: db
    networks:
      - cgnet
    ports:
      - 3306:3306
    volumes:
      - ./data:/var/lib/mysql
      - ./tools/database:/docker-entrypoint-initdb.d
    env_file:
      - .serve.env

  client:
    extends:
      file: .docker/compose/common/preset.app.dev.yml
      service: app
    container_name: client
    networks:
      - cgnet
    entrypoint: sh entrypoint/client.sh
    ports:
      - 3000:3000

  server:
    extends:
      file: .docker/compose/common/preset.app.dev.yml
      service: app
    container_name: server
    networks:
      - cgnet
    entrypoint: sh entrypoint/server.sh
    ports:
      - 6868:6868
    depends_on:
      - mysql

networks:
  cgnet:
    driver: bridge
