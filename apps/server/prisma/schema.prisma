generator client {
  provider = "prisma-client-js"
}

datasource database {
  provider = "mysql"
  url      = env("DATABASE_URL")
}
// Users model

model users {
  username  String  @id @database.VarChar(14)
  password  String  @database.VarChar(14)
  avatarUrl String? @database.VarChar(100)
}
